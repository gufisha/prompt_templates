from setuptools import setup, find_packages

setup(
    name='prompt_templates',  # Replace with a unique name
    version='0.1.0',
    description='A prompt-based module for generating styled outputs',
    author='gufisha',
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],  # Add dependencies if needed
)